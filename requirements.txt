# requirements.txt — Autonomous Driving Safety Demo
# Install with:  pip3 install -r requirements.txt

# Core vision & ML
opencv-python>=4.9.0
tensorflow>=2.15.0         # includes Keras; use tensorflow-macos on Apple Silicon
numpy>=1.24.0

# Google APIs (optional — needed only if GOOGLE_API_KEY is set)
googlemaps>=4.10.0
